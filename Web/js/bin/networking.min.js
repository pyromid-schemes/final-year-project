function openWebSocket(config){var ipAddress=document.getElementById("url").value;ws=new WebSocket("ws://"+ipAddress+":9998"),ws.onopen=function(){readyToSend=!0,config.onOpen()},ws.onmessage=function(evt){var reader=new FileReader;reader.addEventListener("loadend",function(e){var msg=JSON.parse(e.target.result.substring(1));if(msg.hasOwnProperty("command"))switch(msg.command){case"worldStatus":config.onMessage.worldStatus(msg.objects);break;case"vrPosition":config.onMessage.vrPosition(msg.position)}}),reader.readAsText(evt.data)},ws.onclose=function(){readyToSend=!1,config.onClose()}}function closeWebSocket(){ws.close()}function sendMessage(msg){readyToSend&&ws.send(" "+msg)}var UnityClient={buildCommand:function(objectId,xPos,zPos){var command={command:"build",options:{objectId:objectId,xPos:xPos,zPos:zPos}};sendMessage(JSON.stringify(command))},spawnMobCommand:function(objectId,xPos,zPos,id){var command={command:"spawnMob",options:{objectId:objectId,xPos:xPos,zPos:zPos,id:id}};sendMessage(JSON.stringify(command))}},ws,readyToSend=!1;